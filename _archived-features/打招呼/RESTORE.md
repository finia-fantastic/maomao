# 打招呼 动作

## 恢复方法
1. 把 `打招呼.vrma` 复制回 `packages/stage-ui-three/src/assets/vrm/animations/dances/`
2. 在 `apps/stage-tamagotchi/src/renderer/composables/useIdleGestures.ts` 中取消注释 `'打招呼'`

## 修改文件
- 移动: `dances/打招呼.vrma` → `_archived-features/打招呼/`
- 修改: `useIdleGestures.ts` — 注释掉 `'打招呼'`
